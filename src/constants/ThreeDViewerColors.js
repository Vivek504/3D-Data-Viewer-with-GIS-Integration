// Defines color options for point cloud rendering
export const POINT_CLOUD_COLORS = Object.freeze({
    DEFAULT: "#FFFFFF"
});

// Defines background color options for the 3D scene
export const SCENE_BACKGROUND_COLORS = Object.freeze({
    DEFAULT: "#111827"
})