// Constants for 3D settings
export const THREE_D_SETTINGS = Object.freeze({
    CAMERA_SETTINGS: Object.freeze({
        FOV: 75,
        NEAR: 0.1,
        FAR: 1000
    }),
    POINT_SIZE: Object.freeze({
        XYZ: Object.freeze({
            DEFAULT: 0.005
        })
    })
});